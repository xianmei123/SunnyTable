<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evigel.sunnytable.mapper.MlineMapper">
    <select id= "getLines" resultType="Integer">
        SELECT
            line
        FROM
            mline
        WHERE
            mid = '${mid}'
        ORDER BY mline.no ASC

    </select>
</mapper>
